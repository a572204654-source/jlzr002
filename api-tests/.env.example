# API测试环境配置

# 后端API地址（本地开发）
API_BASE_URL=http://localhost:80

# 测试用微信登录code（需要真实的微信code，或者使用mock的openid）
# 注意：微信code只能使用一次，5分钟内有效
# 如果没有真实code，可以先手动创建测试用户，然后使用下面的token
WECHAT_CODE=

# 或者直接使用已有的JWT token（如果已经登录过）
TEST_TOKEN=

# 测试数据前缀（避免污染数据库）
TEST_PREFIX=[API测试]

